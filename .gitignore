# Rust
/target/
**/*.rs.bk
*.pdb

# Env / secrets — never commit; see markdown/DEPLOYMENT.md for the .env contract
.env
.env.*
!.env.example

# Editor / OS
.DS_Store
*.swp
.idea/
.vscode/

# Local scratch
/tmp/
*.log
# ...but committed test fixtures (captured/crafted log samples) are intentional:
!crates/*/tests/fixtures/**
